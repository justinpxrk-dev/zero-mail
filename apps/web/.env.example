DATABASE_URL=

# Better Auth — server secret (generate: openssl rand -base64 32)
BETTER_AUTH_SECRET=
BETTER_AUTH_URL=http://localhost:3000

# Google OAuth client (GCP Console → APIs & Services → Credentials)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Gmail Pub/Sub topic for users.watch (projects/<project-id>/topics/<topic>)
GMAIL_PUBSUB_TOPIC=
