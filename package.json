{
  "name": "zero",
  "private": true,
  "license": "SEE LICENSE IN LICENSE",
  "type": "module",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "prepare": "if [ -d .git ]; then lefthook install; fi",
    "dev": "pnpm --filter @zero/web dev",
    "check": "pnpm format:check && pnpm lint && pnpm typecheck && pnpm build && pnpm test",
    "fix": "pnpm lint:fix && pnpm format",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "eslint . && markdownlint-cli2",
    "lint:fix": "eslint . --fix && markdownlint-cli2 --fix",
    "typecheck": "pnpm --filter @zero/core typecheck",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "db:generate": "pnpm --filter @zero/web db:generate",
    "db:migrate": "pnpm --filter @zero/web db:migrate",
    "db:push": "pnpm --filter @zero/web db:push",
    "db:studio": "pnpm --filter @zero/web db:studio",
    "adr:new": "pnpm dlx log4brains@1 adr new",
    "adr:preview": "pnpm dlx log4brains@1 preview",
    "adr:build": "pnpm dlx log4brains@1 build"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@tsconfig/next": "^2.0.6",
    "@tsconfig/node24": "^24.0.4",
    "@tsconfig/strictest": "^2.0.8",
    "@types/node": "^24.12.4",
    "eslint": "^10.4.0",
    "eslint-plugin-simple-import-sort": "^13.0.0",
    "eslint-plugin-tsdoc": "^0.5.2",
    "globals": "^17.6.0",
    "markdownlint-cli2": "^0.22.1",
    "prettier": "^3.8.3",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.59.4"
  }
}
